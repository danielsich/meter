{
  "name": "meter",
  "version": "1.0.0",
  "description": "Static dashboard for the clockwork CLI (schema clockwork/v1).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-data.mjs && vite",
    "build": "node scripts/prepare-data.mjs && tsc && vite build",
    "test": "node --test 'src/**/*.test.ts'",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.6.0",
    "vite": "^8.1.3"
  }
}
